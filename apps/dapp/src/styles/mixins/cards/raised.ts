import { css } from 'styled-components';

const raisedStyles = css`
  box-shadow: -10px -10px 40px rgba(255, 255, 255, 0.07),
    10px 10px 40px rgba(0, 0, 0, 0.4),
    inset 5px 5px 10px rgba(255, 255, 255, 0.04),
    inset -5px -5px 10px rgba(0, 0, 0, 0.2);
`;

export default raisedStyles;
