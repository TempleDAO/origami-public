pragma solidity ^0.8.0;
// SPDX-License-Identifier: AGPL-3.0-or-later

// Required for typechain to pull it in, so we can deploy
import { MorphoChainlinkOracleV2 } from "@morpho-org/morpho-blue-oracles/src/morpho-chainlink/MorphoChainlinkOracleV2.sol";

contract MorphoOracleDeployer {
}